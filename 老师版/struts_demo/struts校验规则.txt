struts的校验规则：
1.类型转换的校验（类型转换的校验）
2.Struts的核心验证框架（数据校验）
验证框架分为：字段校验器（field）和非字段校验器（validator）
非字段校验器优先于字段校验器先校验
3.手动校验规则（完成复杂的校验规则）
手动校验就是在action中重写父类的validate方法或执行编写
validateXxx方法进行校验。当这两个方法都通过后，整个验证流程将通过。接下来直接调用action中的方法。

说明：当任何一个校验方法没有通过的时候，是不会执行action中的方法
